[phases.setup]
nixPkgs = ['nodejs', 'pnpm', 'python3']

[phases.install]
cmds = ['cd ../.. && pnpm install --frozen-lockfile']

[phases.build]
cmds = ['cd ../.. && cd backend/gateway && pnpm build']

[start]
cmd = 'pnpm start'
