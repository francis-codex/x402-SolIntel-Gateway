{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @x402-solintel/types build && pnpm --filter dashboard build",
    "nixPkgs": ["nodejs", "pnpm"]
  },
  "deploy": {
    "startCommand": "cd frontend/dashboard && pnpm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
