{
  "name": "@x402-solintel/types",
  "version": "1.0.0",
  "description": "Shared TypeScript types for x402-SolIntel-Gateway",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
