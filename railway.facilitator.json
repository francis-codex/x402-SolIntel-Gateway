{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @x402-solintel/types build && pnpm --filter facilitator build",
    "nixPkgs": ["nodejs", "pnpm", "python3"]
  },
  "deploy": {
    "startCommand": "cd backend/facilitator && pnpm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
